<h1><%= link_to "Wikinimous", articles_path %></h1>

<ul>
  <% @articles.each do |article| %>
      <li><h2><%= link_to article.title, article_path(article) %> / <%= link_to "Delete", article_path(article), data: {turbo_method: :delete, turbo_confirm: "Are you sure?"} %></h2><p><%= article.content %></p></li>
  <% end %>
</ul>

<%= link_to "New Article", new_article_path %>
